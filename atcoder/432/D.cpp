#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, x, y; cin >> n >> x >> y;
    set<pair<int, int>> pos;
    for (int i = 0; i < x; i++) for (int j = 0; j < y; j++) pos.insert(make_pair(i, j));

}
